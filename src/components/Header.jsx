import React from 'react';

function Header() {
  return (
    <div>
      <button
        type="button"
        data-testid="profile-top-btn"
        className="profileButton"
      >
        Search
      </button>

    </div>
  );
}

export default Header;
